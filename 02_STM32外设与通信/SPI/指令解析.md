### 解除掉电状态/唤醒芯片指令

**Power-down（掉电/深睡眠模式）**：Flash 在执行了 `B9h`（Power-down）指令后，会进入极其省电的休眠状态，此时芯片拒绝接收所有的读写/擦除指令（除了唤醒指令）。

**Release Power-down（解除唤醒）**：通过发送指令 ABh，把 Flash 从低功耗休眠状态中“拉起来”，恢复到正常的待机状态（Standby mode），之后才能正常进行读、写、擦除。