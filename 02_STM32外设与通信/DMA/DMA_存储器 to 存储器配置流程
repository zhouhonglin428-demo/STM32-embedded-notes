# M to M配置流程

> **核心概念**：可从fFash到SRAM也可从SRAM到SRAM
>
> ​		  M to M(存储器到存储器传输必须使用DMA2)

**配置步骤**： 

1. 准备数据源与目标：

			* 确定源数据地址（如 Flash 常量数组或 SRAM 变量） 

* 确定目标数据地址（如 SRAM 接收缓冲区）

2. 使能 DMA2 外设时钟 (RCC_AHB1PeriphClockCmd) 

3. 复位目标 DMA 数据流 (DMA_DeInit)，清除残留状态 

   (确保清空历史残余配置完成，防止影响寄存器的当前状态，进而导致配置失或数据传输异常)

4. 配置 DMA 初始化结构体参数  

5. 调用 DMA_Init() 将配置写入硬件寄存器

6. 使能 DMA 数据流 (DMA_Cmd)，正式启动传输 

7. (可选)数据校验：编写比较函数校验源数据与目标数据是否一致



# M to P配置过程（DMA搬送数据实验）

解析：

1. 定义5000个数据通过DMA直接发送到串口的TX
2. 串口再发送到GPIO
3. GPIO发送到我们的电脑上位机
